{{template "admin" .}}

{{define "page-title"}}
    All Reservations
{{end}}

{{define "content"}}
    <div class="col-md-12">
        {{$res := index .Data "reservations"}}
        <table class="table table-striped table-hover" id="all-res">
            {{range $res}}
            
            {{end}}
        </table>

    </div>
{{end}}